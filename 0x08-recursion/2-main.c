#include "main.h"
#include <stdio.h>

/**
 * main - Check the code
 *
 * Return: Always 0
 */
int main(void)
{
    int n;

    n = _strlen_recursion("Corbin Coleman");
    printf("%d\n", n);
    return (0);
}
